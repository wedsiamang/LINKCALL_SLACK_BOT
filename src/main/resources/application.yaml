spring:
 application:
   name: linkcall
 datasource:
   url: jdbc:h2:file:./data/keywords
   driver-class-name: org.h2.Driver
 jpa:
   hibernate:
    ddl-auto: update

slack:
    bot-token: ${SLACK_BOT_TOKEN}
    app-token: ${SLACK_APP_TOKEN}

